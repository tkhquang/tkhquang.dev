[package]
name = "ljoss-search"
version = "1.0.0"
edition = "2021"
publish = false

[lib]
# Both targets share the builder, tokenizer, and query implementation.
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
